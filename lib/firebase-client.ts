/**
 * Firebase client SDK init — reads, realtime subscriptions, and auth.
 * Frontend only (CONVENTIONS §8). Uses NEXT_PUBLIC_* config only.
 *
 * TODO: init app/auth/firestore/rtdb against the installed `firebase` version.
 * Verify the current SDK init API before implementing (AGENTS §4).
 */

export {};
