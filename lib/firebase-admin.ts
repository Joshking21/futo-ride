/**
 * Firebase Admin SDK init — privileged writes + ID-token verification.
 * Backend only (CONVENTIONS §8). Reads FIREBASE_ADMIN_* secrets from process.env.
 *
 * TODO: init the Admin app against the installed `firebase-admin` version.
 * Verify the current init + credential API before implementing (AGENTS §4).
 */

export {};
